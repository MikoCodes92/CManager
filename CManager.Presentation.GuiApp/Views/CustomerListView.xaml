<UserControl x:Class="CManager.Presentation.GuiApp.Views.CustomerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CManager.Presentation.GuiApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--Header-->
        <Border Grid.Row= "0" Background="#007ACC" Padding="10">
            <TextBlock Text="{Binding Title}"
                          Foreground="White"
                          FontSize="18"
                          FontWeight="Bold"/>
        </Border>

        <!--status-->
        <Border Grid.Row="1" Background="#F0F0F0" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding StatusMessage}" FontStyle="Italic"/>
                <ProgressBar Width="100"
                             Height="10"
                             Margin="10,0,0,0"
                             IsIndeterminate="{Binding IsBusy}"
                             Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisiblilityConverter}}"/>
            </StackPanel>
        </Border>

        <!--Customer List-->
        <ListView Grid.Row="1"
                  Margin="0,35,20,21"
                  ItemsSource="{Binding Customers}"
                  SelectedItem="{Binding SelectedCustomer}" Grid.RowSpan="2">
            <ListView.View>
              <GridView>
                <GridViewColumn Header="First Name" Width="50" DisplayMemberBinding="{Binding FirstName}"/>
                <GridViewColumn Header="LastName" Width="200" DisplayMemberBinding="{Binding LastName}"/>
                <GridViewColumn Header="Email" Width="250" DisplayMemberBinding="{Binding Email}"/>
                <GridViewColumn Header="PhoneNumber" Width="150" DisplayMemberBinding="{Binding PhoneNumber}"/>
                <GridViewColumn Header="Address" Width="150" DisplayMemberBinding="{Binding Address}"/>
                <GridViewColumn Header="City" Width="150" DisplayMemberBinding="{Binding City}"/>
                <GridViewColumn Header="Street" Width="150" DisplayMemberBinding="{Binding Street}"/>
              </GridView>
            </ListView.View>
        </ListView>

        <!--Button-->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Refresh" Command="{Binding LoadCustomersCommand}" Background="#4CAF50" Foreground="White"/>
            <Button Content="Delete" Command="{Binding DeleteCustomerCommand}"  Background="#F44336" Foreground="White"/>
            <Button Content="Edit" Command="{Binding ViewDetailsCommand}"  Background="#2196F3" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>
